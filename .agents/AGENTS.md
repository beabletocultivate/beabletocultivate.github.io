# Project Guidelines

- **Git Commits & Pushes**: NEVER run `git commit` or `git push` unless the user explicitly commands you to do so (e.g., "commit", "git commit", "push"). Always leave changes uncommitted for user review.
